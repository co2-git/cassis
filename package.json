{
  "name": "cassis",
  "version": "1.0.0",
  "description": "CSS generator in JavaScript",
  "main": "dist/lib/cassis.js",
  "scripts": {
    "test": "redtea dist/test",
    "babel": "babel --out-dir dist app",
    "babel-watch": "babel --watch --out-dir dist app",
    "postinstall": "npm run babel",
    "browser": "node_modules/.bin/browserify dist/browser.js -o bundle.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "css": "^2.2.1"
  },
  "devDependencies": {
    "babel-cli": "^6.5.1",
    "babel-preset-es2015": "^6.5.0",
    "babel-preset-stage-0": "^6.5.0",
    "browserify": "^11.2.0",
    "colors": "^1.1.2",
    "redtea": "^3.4.1",
    "should": "^8.2.2"
  }
}
